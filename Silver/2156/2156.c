#include <stdio.h>

int main() {
  int count;
  scanf("%d", &count);
  int arr[count];
  int dp[count][count];
  for (int i = 0; i < count; i++) scanf("%d", &arr[i]);
  for (int i = 0; i < 2; i++) {
    for (int ii = 0; ii < count; ii++) {

    }
  }
}